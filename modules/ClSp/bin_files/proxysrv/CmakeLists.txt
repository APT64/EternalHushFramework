cmake_minimum_required (VERSION 3.8)

project(proxysrv VERSION 1.0)
set (CMAKE_CXX_STANDARD 20)

file(GLOB SOURCE_FILE 
*.c
)
file(GLOB SHARED_SOURCE_FILE 
../shared/*.c
)
file(GLOB HEADER_FILE 
inc/*.h
)


add_library(proxysrv SHARED
${SOURCE_FILE}
${SHARED_SOURCE_FILE}
${HEADER_FILE})

target_include_directories(proxysrv 
PRIVATE 
inc
)
